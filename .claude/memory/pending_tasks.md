---
name: pending-tasks
description: تذكيرات ومهام مؤجَّلة في مشروع قدرات المغربي — اقرأها عند بداية أي جلسة، وقبل بناء أي نسخة جديدة من التطبيق، وعند السؤال «إيه اللي فاضل؟».
type: project
---

## 📦 نسخة التطبيق 1.1.2 (14) — منشورة ✅
- **iOS:** **اتقبلت ونُشرت على App Store** (أكّده كريم 2026-09-05).
- **Android:** منشورة على موقع المنصة (commit `fd59be5`).
- آخر commit على `flutter-app`: **`f1d645d`**. التفاصيل في [releases](releases.md).
- **بعد موافقة أبل:** كريم يجرّب على الآيفون — تسجيل الشاشة، تحذير اللقطة، والعلامة المائية.

## ⏳ مستني موافقة كريم — صفحة سجل لقطات الشاشة في لوحة الأدمن
كشف اللقطة وتسجيلها اتنفّذ بالكامل (جدول `public.screenshot_events` + RLS متحقَّق منها)، **بس مفيش واجهة تعرض السجل**. البيانات موجودة في القاعدة والأدمن يقدر يقراها، لكن مافيش صفحة.
- **ليه مؤجَّلة:** صفحة جديدة في لوحة الأدمن = خارج نطاق «عدّل اللي فيه مشكلة بس»، فمحتاجة موافقته.
- **المطلوب لو وافق:** صفحة تعرض (الطالب · الدرس · التاريخ · المنصة) مرتّبة بالأحدث، مع فلتر بالطالب. التفاصيل في [screenshot_protection](screenshot_protection.md).

## ⏳ مؤجَّل بطلب كريم — «حيلة تطبيقات البنوك» لمنع لقطة الشاشة على iOS
عجبته الفكرة وطلب حفظها وتذكيره بها. التفاصيل والمخاطر وطريقة التنفيذ الآمنة في [screenshot_protection](screenshot_protection.md).

## ⏳ مؤجَّل — العلامة المائية على فيديو شرح الإجابة
فيديو **شرح الإجابة** في الاختبارات (`quiz_result_screen.dart` و`QuizResult.tsx`) مالوش علامة مائية، لأنه محتاج تمرير هوية الطالب خلال مسار الاختبارات. فيديوهات الدروس (الأصل الأهم) مغطّاة. **محتاج قرار كريم.**

## مؤجَّل — مربوط بنشر 1.1.2
### تنظيف آخر بقايا «ولي الأمر» من قاعدة البيانات
- `public.get_my_access_profile()` و`public.set_my_active_portal(text)` لسه فيهما فرع `'parent'`، والأولى بترجع عمود `can_use_parent_portal`.
- ✅ الخطوة الأولى اتعملت: الحقل اتشال من كود التطبيق في `2bd7b4d`.
- **الباقي:** بعد ما 1.1.2 تُنشر وأغلب الطلاب يحدّثوا → اشيل الفرع والعمود من الدالتين. (النسخ القديمة المنشورة لسه بتقرا الحقل عند كل تسجيل دخول.)

## 🔴 قبل نشر تطبيق أندرويد في الإنتاج — إلزامي
التطبيق لسه في **الاختبار المغلق** و0 تثبيتات، فمفيش ضرر حاليًا. لكن **قبل النشر في الإنتاج لازم يتضاف `GOOGLE_PLAY_SERVICE_ACCOUNT_JSON`** في Supabase Secrets، وإلا أول طالب يشتري من أندرويد هيتخصم منه والمحتوى يفضل مقفول. التفاصيل في [google_play_status](google_play_status.md) و[store_purchase_failure](store_purchase_failure.md).

## على المنصة — محتاج قرار المستخدم قبل التنفيذ
### صفحة إعدادات المنصة في الأدمن شبه وهمية
`admin/AdminSettings.tsx` → زر «حفظ التغييرات» بينادي `save()` اللي بيعرض رسالة نجاح **بدون أي حفظ**. ومفتاحا «إتاحة التسجيل الجديد» و«التنبيهات البريدية» غير مربوطين بأي حاجة. (وضع الصيانة وحده حقيقي.)
**الخيارات:** ربطهم فعليًا بقاعدة البيانات، أو إخفاء الزر والمفتاحين لحد ما يتربطوا.

## ✅ اتصلّحت — بنية المجلدات على الماك بوك اير (2026-09-02)
كانت المشكلة: مستودع المنصة جذره `قدرات المغربي` نفسه والكود مسطَّح في الجذر، ومجلد `Platform/` نسخة قديمة (28–31 أغسطس) غير متتبَّعة في Git، و194 ملف متتبَّع كانوا محذوفين من شجرة العمل.

**اللي اتعمل:**
1. `git checkout -- .` رجّع الـ194 ملف وطابق الشجرة مع `origin/main`.
2. `Platform/` القديم اتسمّى `_old_Platform_backup/` (**اتحفظ ولم يُحذف** — جواه `node_modules` و`dist` و`_to_delete`).
3. المستودع الحقيقي بالكامل (كل الملفات المتتبَّعة + `.git` + `.claude` + `.env` + `.gitignore`) اتنقل من الجذر إلى `Platform/` الجديد.

**البنية النهائية المعتمدة:**
```
/Volumes/MacBook SSD/Kareem-AI/قدرات المغربي/
├── Platform/               ← مستودع المنصة (فرع main) — .git هنا
├── App/                    ← مستودع التطبيق (يتتبّع origin/flutter-app)
└── _old_Platform_backup/   ← نسخة قديمة محفوظة، لا تُستخدم
```
- ⚠️ `Platform/` الجديد **مفيهوش `node_modules`** — أول `npm run dev`/`build` محتاج `npm install` الأول.
- مستودعان منفصلان بـ`.git` مستقلّة لكن نفس الـremote `KareemPro-Hub/qudrat-maghrabi`.
- `App/` كان **متأخّر 12 commit** (كان على `4589d19` ونسخة `1.1.0+12`) — اتعمل له fast-forward لـ`f1d645d` = `1.1.2+14`، و`app_metadata.dart` مطابق لـ`pubspec.yaml`.
- ✅ الفرع المحلي في `App/` اتسمّى **`flutter-app`** (كان `main` وبيتتبّع `origin/flutter-app` — مربك وخطر عند الدفع). `git branch -m` بيفشل على الـVM لأنه محتاج حذف الـref؛ الحل: `mv .git/refs/heads/<old> .git/refs/heads/<new>` + نفس الشيء لـ`.git/logs/refs/heads/` + `git symbolic-ref HEAD refs/heads/<new>` + ضبط `branch.<new>.remote/merge`.
- ✅ `npm install` اتعمل في `Platform/` (189 حزمة) و`npm run build` نجح. الحزم متعددة المنصات (فيها `darwin-arm64`/`darwin-x64`) فهتشتغل على الماك عادي.

### 🔴 حيلة جديدة — git لا يستطيع استبدال ملف (مش بس الأقفال)
الـVM ممنوع من الحذف، فـ`git merge`/`checkout` بيفشل بـ`error: unable to unlink old '<file>'` على أي ملف **متتبَّع هيتعدّل**، وبـ`untracked working tree files would be overwritten` على أي ملف غير متتبَّع. الحل بدون صلاحيات حذف: انقل الملفات المذكورة في الخطأ إلى `_to_delete/` بـ`mv` وكرّر الأمر في حلقة لحد ما ينجح — git بيعيد إنشاءها. (اتستخدم لتحديث `App/`؛ الملفات القديمة محفوظة في `App/_to_delete/`.)

## 📦 نسخة 1.1.3 (15) — أُرسلت للمراجعة ✅ (2026-09-05)
- **الحالة: «Waiting for Review»**، إصدار تلقائي بعد الموافقة (`AFTER_APPROVAL`)، بلا Phased Release.
- آخر commit على `flutter-app`: **`8b99fe3`**.
- المحتوى:
  1. صورة السؤال في شاشة مراجعة الإجابات (`74f6462`).
  2. زر الدعم زجاجي شفاف بدل الأبيض الصريح (`74f6462`).
  3. رفع الرقم إلى `1.1.3+15` (`8b99fe3`).
- `dart analyze` نضيف و51 اختبارًا ناجحة.
- نص «ما الجديد» المستخدَم (فصحى، ثلاث نقاط):
  > • عند مراجعة إجابات الاختبار، صار السؤال يظهر كاملًا فوق إجابتك والإجابة الصحيحة.
  > • تحسين شكل زر الدعم في شاشات الدخول.
  > • إصلاحات وتحسينات في الأداء.
- على المنصة نُشر نفس تعديل صورة السؤال (commit `fdb4ac9`).
- ✅ **Android: منشور على موقع المنصة** — commit `56bdc42` على `main`. md5 القديم `52a9e27d…` والجديد `606cd389…`، و`android-version.json` بقى 1.1.3/15.
- **الباقي:** انتظار قرار أبل على iOS.

## ⏳ مستني قرار كريم — تنبيه التحديث داخل التطبيق
الطلاب لا يعرفون أن نسخة جديدة نزلت على App Store، فيظلون على القديمة. لا Apple
ولا Google يجبران على التحديث؛ الحل المعتاد أن التطبيق يسأل السيرفر عند الإقلاع
عن آخر نسخة ويقارنها بنسخته.

**الخياران المعروضان عليه (لم يجب بعد):**
- **أ) تنبيه عادي** — «فيه تحديث جديد» + زر يفتح المتجر، ويقدر يقفلها ويكمل.
- **ب) إجباري** — لا يعدّي حتى يحدّث.

**نصف الشغل جاهز:** `app_metadata.dart` فيه رقم النسخة، والموقع فيه
`public/downloads/android-version.json`. الناقص: مصدر لآخر نسخة (جدول في Supabase
أو توسيع ملف الـJSON) + شاشة/لافتة التنبيه في التطبيق.
⚠️ لافتة «حدّث التطبيق» ظهرت كاذبة قبل كده بسبب عدم تطابق `app_metadata.dart`
مع `pubspec.yaml` — أي حل لازم يقارن بالسيرفر لا بقيمة محلية.

## 📋 بنود التطبيق المرشَّحة للتحديث القادم — كريم طلب حفظها كلها (2026-09-11)
مرتَّبة بالأولوية. كلها **لم تُنفَّذ بعد**، وكلها في مستودع التطبيق.

| # | البند | الملف | الحجم |
|---|---|---|---|
| 1 | علامة مائية على فيديو شرح الإجابة (فيديوهات الدروس مغطّاة) | `quiz_result_screen.dart` + `QuizResult.tsx` | متوسط |
| 2 | الدرس يُحتسب مكتملًا عند 95٪ بدل 100٪ — 6 حالات عالقة عند 3 طلاب | منطق `lesson_progress` | صغير |
| 3 | زر الرجوع في مشغّل الدرس يتجمّد لأن `_close()` ينتظر الحفظ بلا مهلة | `lesson_player_screen.dart` | صغير |
| 4 | «تذكرني» في تسجيل الدخول بلا أي أثر — يُربط أو يُحذف | `login_screen.dart` | صغير |
| 5 | نص استثناء إنجليزي خام يظهر للطالب | `quiz_attempt_screen.dart:242` · `quiz_result_screen.dart:64` | صغير |
| 6 | `launchUrl` بلا try/catch في 6 مواضع | متفرّق | صغير |
| 7 | خيار رابع فارغ يظهر في الأسئلة الثلاثية | `supabase_student_quiz_repository.dart:179` | صغير |
| 8 | «حيلة تطبيقات البنوك» لمنع لقطة الشاشة على iOS | ملف Swift معزول | ⚠️ خطر |
| 9 | `WebView` بلا `dispose()` | `lesson_player_screen.dart` | صغير |
| 10 | `Image.network` بلا `cacheWidth` في 3 مواضع | متفرّق | صغير |
| 11 | 6600ms قبل بدء أي عمل في شاشة البداية | `brand_launch_gate.dart:32` | صغير |
| 12 | الوضع الداكن في `subscription_screen.dart` (كود ميت — يُحذف أو يُصلَح) | — | صغير |

**ترشيحي:** 2 و3 و5 و6 و7 معًا — كلها صغيرة ومأمونة.
تفاصيل 1 و8 في [screenshot_protection](screenshot_protection.md)، وباقي البنود من [audit_2026_08_31](audit_2026_08_31.md).

## ✅ اتنفّذ 2026-09-11 — غير مبنيّ بعد (على `flutter-app`)
1. **دعم مدير كلمات المرور على iOS:** الحقول كان عندها `autofillHints` لكن **بدون `AutofillGroup`** وبدون `TextInput.finishAutofillContext()` — ولذلك آيفون لم يكن يقترح الحساب ولا يعرض حفظه. أُضيف الاثنان في `login_screen` و`register_screen` و`reset_password_screen`.
2. **تباين الوضع الداكن:**
   - `QmColors.purple` (#7A2DD6) كان ثابتًا حتى فوق الخلفية الداكنة ⇒ تباين **2.2:1**. أُضيف **`QmColors.accent`** (داكن: #B98CF5 ⇒ **7.5:1**) وطُبِّق على 35 موضعًا، و`primary` في الثيم الداكن صار نفس اللون. **القاعدة:** `purple` للتدرّجات والخلفيات الممتلئة فقط، و`accent` للنصوص والأيقونات والحدود فوق الأسطح.
   - أُضيفت تدرّجات الحالات: `successTint` · `successTintStrong` · `errorTint` · `errorTintStrong` · `pinkTint` — تُعتِم في الوضع الداكن.
   - **أخطر خطأين أُصلحا:** الإجابة المختارة في الاختبار (خلفية وردية فاتحة ثابتة + نص `textPrimary` فاتح = غير مقروء إطلاقًا)، وبطاقة نتيجة الاختبار (تدرّج فاتح ثابت + نص فاتح).
- ✅ `flutter analyze` نضيف و**51 اختبارًا** ناجحة.
- ⚠️ **لم يُرفع رقم النسخة بعد** — `pubspec.yaml` و`app_metadata.dart` لسه `1.1.3+15` وهي **منشورة فعلًا**. قبل أي بناء لازم يبقى **`1.1.4+16`**.

## ملاحظات دائمة عند البناء
- `pubspec.yaml` و`lib/core/config/app_metadata.dart` لازم يتطابقا.
- الجلسة مش قادرة تشغّل `flutter analyze` أو `flutter test` (مفيش Flutter/Dart على الجهاز) — تُطلب من كريم.
- حيلة قفل git وأوامر البناء الكاملة في [env_limits](env_limits.md) و[releases](releases.md).

## 🗑️ تذكير — نسخة احتياطية مؤقتة لازم تتمسح
`/Volumes/MacBook SSD/Kareem-AI/قدرات المغربي/_old_Platform_backup/` — **878 ميجا**، بقايا إعادة ترتيب المجلدات في 2026-09-02.
- **مفيش فيها أي حاجة مش موجودة في Git** — اتفحصت وقتها.
- **الخطة المتفق عليها مع كريم:** بعد أسبوع (من ~2026-09-09) ولو كل حاجة ماشية تمام، يمسحها بنفسه من Finder.
- **فكّره بيها** أول ما يسأل «إيه اللي فاضل؟» بعد التاريخ ده.
- ✅ مجلدات `_to_delete` و`.git/_stale` في Platform وApp اتمسحت خلاص في 2026-09-02.

## 🔴 فيديوهات «عرفني الإجابة الصحيحة» ناقصة — سبب شكاوى الطلاب (2026-09-09)
الزر بيظهر **فقط** لو السؤال متحطّ له `explanation_video_id`. الوضع في الاختبارات المنشورة:
| الاختبار | أسئلة لها فيديو |
|---|---|
| الأعداد العشرية | 10/10 ✅ |
| جمع وطرح الكسور | 8/10 |
| ضرب وقسمة الكسور | 1/10 |
| مقارنة الكسور | **0** |
| مقارنة الجذور | **0** |
| جمع وطرح الجذور | **0** |
| ضرب وقسمة الجذور (اختباران) | **0** |

**ليست ثغرة برمجية** — المسار سليم ومختبَر ([quiz_explanation_video](quiz_explanation_video.md)). المطلوب إدخال أرقام Bunny من لوحة الأدمن.
**شكوى موثّقة:** الطالب `rakanalajlani51@gmail.com` (راكان العجلاني) — حلّ «مقارنة الكسور» ٣ مرات في 2026-09-08 ولم يجد الزر. حسابه سليم تمامًا: اشتراك Apple شهري ساري حتى 2026-10-03، والوصول مفتوح بالكامل (تحقُّق بانتحال الهوية: 8 دروس + 8 اختبارات + الملفات).

## ⚠️ الدرس لا يُحتسب مكتملًا إلا عند 100٪ بالضبط
`lesson_progress.completed` يبقى false عند 95–99٪، والفيديو غالبًا يقف قبل الثانية الأخيرة. **6 صفوف عند 3 طلاب** عالقة كده (فحص 2026-09-09). الحل المقترح: اعتبار الدرس مكتملًا عند ≥95٪. **لم يُنفَّذ — يحتاج قرار كريم.**

## قرارات مغلقة — لا تُفتح ولا يُذكَّر بها
- **ميزة ولي الأمر:** ألغاها المستخدم نهائيًا (2026-09-01). الباقي الوحيد هو بند قاعدة البيانات أعلاه.
- **حماية كلمات المرور المسرّبة في Supabase:** قرار المستخدم صراحةً تجاهلها تمامًا وعدم ذكرها مرة أخرى.
- **عطل دفع Apple:** اتحل نهائيًا 2026-09-02 وثبت بعملية شراء حقيقية. لا يُعاد فتحه.
