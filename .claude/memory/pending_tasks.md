---
name: pending-tasks
description: تذكيرات ومهام مؤجَّلة في مشروع قدرات المغربي — اقرأها عند بداية أي جلسة، وقبل بناء أي نسخة جديدة من التطبيق، وعند السؤال «إيه اللي فاضل؟».
type: project
---

## 📦 نسخة التطبيق 1.1.2 (14) — اتبنت واترفعت ✅
- **iOS:** أُرسلت للمراجعة — الحالة «Waiting for Review».
- **Android:** منشورة على موقع المنصة (commit `fd59be5`).
- آخر commit على `flutter-app`: **`f1d645d`**. التفاصيل في [releases](releases.md).
- **بعد موافقة أبل:** كريم يجرّب على الآيفون — تسجيل الشاشة، تحذير اللقطة، والعلامة المائية.

## ⏳ مستني موافقة كريم — صفحة سجل لقطات الشاشة في لوحة الأدمن
كشف اللقطة وتسجيلها اتنفّذ بالكامل (جدول `public.screenshot_events` + RLS متحقَّق منها)، **بس مفيش واجهة تعرض السجل**. البيانات موجودة في القاعدة والأدمن يقدر يقراها، لكن مافيش صفحة.
- **ليه مؤجَّلة:** صفحة جديدة في لوحة الأدمن = خارج نطاق «عدّل اللي فيه مشكلة بس»، فمحتاجة موافقته.
- **المطلوب لو وافق:** صفحة تعرض (الطالب · الدرس · التاريخ · المنصة) مرتّبة بالأحدث، مع فلتر بالطالب. التفاصيل في [screenshot_protection](screenshot_protection.md).

## ⏳ مؤجَّل بطلب كريم — «حيلة تطبيقات البنوك» لمنع لقطة الشاشة على iOS
عجبته الفكرة وطلب حفظها وتذكيره بها. التفاصيل والمخاطر وطريقة التنفيذ الآمنة في [screenshot_protection](screenshot_protection.md).

## ⏳ مؤجَّل — العلامة المائية على فيديو شرح الإجابة
فيديو **شرح الإجابة** في الاختبارات (`quiz_result_screen.dart` و`QuizResult.tsx`) مالوش علامة مائية، لأنه محتاج تمرير هوية الطالب خلال مسار الاختبارات. فيديوهات الدروس (الأصل الأهم) مغطّاة. **محتاج قرار كريم.**

## مؤجَّل — مربوط بنشر 1.1.2
### تنظيف آخر بقايا «ولي الأمر» من قاعدة البيانات
- `public.get_my_access_profile()` و`public.set_my_active_portal(text)` لسه فيهما فرع `'parent'`، والأولى بترجع عمود `can_use_parent_portal`.
- ✅ الخطوة الأولى اتعملت: الحقل اتشال من كود التطبيق في `2bd7b4d`.
- **الباقي:** بعد ما 1.1.2 تُنشر وأغلب الطلاب يحدّثوا → اشيل الفرع والعمود من الدالتين. (النسخ القديمة المنشورة لسه بتقرا الحقل عند كل تسجيل دخول.)

## 🔴 قبل نشر تطبيق أندرويد في الإنتاج — إلزامي
التطبيق لسه في **الاختبار المغلق** و0 تثبيتات، فمفيش ضرر حاليًا. لكن **قبل النشر في الإنتاج لازم يتضاف `GOOGLE_PLAY_SERVICE_ACCOUNT_JSON`** في Supabase Secrets، وإلا أول طالب يشتري من أندرويد هيتخصم منه والمحتوى يفضل مقفول. التفاصيل في [google_play_status](google_play_status.md) و[store_purchase_failure](store_purchase_failure.md).

## على المنصة — محتاج قرار المستخدم قبل التنفيذ
### صفحة إعدادات المنصة في الأدمن شبه وهمية
`admin/AdminSettings.tsx` → زر «حفظ التغييرات» بينادي `save()` اللي بيعرض رسالة نجاح **بدون أي حفظ**. ومفتاحا «إتاحة التسجيل الجديد» و«التنبيهات البريدية» غير مربوطين بأي حاجة. (وضع الصيانة وحده حقيقي.)
**الخيارات:** ربطهم فعليًا بقاعدة البيانات، أو إخفاء الزر والمفتاحين لحد ما يتربطوا.

## ملاحظات دائمة عند البناء
- `pubspec.yaml` و`lib/core/config/app_metadata.dart` لازم يتطابقا.
- الجلسة مش قادرة تشغّل `flutter analyze` أو `flutter test` (مفيش Flutter/Dart على الجهاز) — تُطلب من كريم.
- حيلة قفل git وأوامر البناء الكاملة في [env_limits](env_limits.md) و[releases](releases.md).

## قرارات مغلقة — لا تُفتح ولا يُذكَّر بها
- **ميزة ولي الأمر:** ألغاها المستخدم نهائيًا (2026-09-01). الباقي الوحيد هو بند قاعدة البيانات أعلاه.
- **حماية كلمات المرور المسرّبة في Supabase:** قرار المستخدم صراحةً تجاهلها تمامًا وعدم ذكرها مرة أخرى.
- **عطل دفع Apple:** اتحل نهائيًا 2026-09-02 وثبت بعملية شراء حقيقية. لا يُعاد فتحه.
