---
name: audit-2026-08-31
description: نتائج الفحص الشامل للمنصة والتطبيق وما عولج منها — قائمة الديون التقنية المتبقية. اقرأها قبل أي شغل تحسيني عام.
type: project
---

> ⚠️ **القائمة دي من 2026-08-31.** بعض بنودها اتحلّت بعد كده — المعلَّم بـ✅ اتأكّد حلّه. الباقي **لسه قائم لكن محتاج موافقة كريم** لأنه خارج نطاق «عدّل اللي فيه مشكلة بس».

## عولجت ✅
- **التطبيق**: `app_metadata.dart` صار مطابقًا لـ`pubspec.yaml` (كان بيسبب لافتة «حدّث التطبيق» الكاذبة ووسم Sentry خاطئ).
- **المنصة**: `api/send-email.ts` — كانت مفتوحة بلا مصادقة لكل الأنواع عدا team_invite. الآن: توكن صالح لكل الأنواع، الأنواع الإدارية (team_invite, admin_broadcast, enrollment, payment_failed, new_lesson) للأدمن فقط، quiz_passed للطالب لنفسه فقط، تحقق من صيغة المستلم، وتهريب كل قيم العميل. Commit bd53cc1.
- ✅ **المنصة #2** — مسارا `/parent` و`/parent/link`: **ساقط**، ميزة ولي الأمر أُلغيت بالكامل (2026-09-01).
- ✅ **التطبيق #4** — طلب رمز Bunny بلا `.timeout`: **اتحل** في 1.1.2 (commit `a02db25`، مهلة 20 ثانية برسالة عربية).
- ✅ **التطبيق #14** — كود ميت `features/parent_home`: ساقط مع إلغاء الميزة.

## متبقّية — المنصة
3. `CurrencySymbol.tsx:6-12` — يعرض ر.س دائمًا؛ `/checkout/:courseId` يعرض «249 ر.س» والخصم بالجنيه.
4. كورسان منشوران بسعر `249.00 EGP` قديم.
5. 16 دالة تحميل بلا try/catch/finally → شاشة تحميل أبدية.
6. `Profile.tsx:150-163` — كلمة المرور الحالية غير متحقَّق منها.
7. `admin/AdminSettings.tsx:44-46` — رسالة نجاح كاذبة ومفتاحان غير مربوطين. (مذكورة كمان في [pending_tasks](pending_tasks.md).)
8. جداول بلا Migration: platform_settings, chapters, lesson_files, discount_codes.
9. `server/paymob.ts:36-39` — سعر صرف 13.55 مثبّت كافتراضي.
10. `admin/AdminNotifications.tsx:135-149` — Promise.all بلا حدّ تزامن.
11. `notify_students_of_new_content()` تحمل EXECUTE لـ anon/public.
12. بسيطة: روابط Footer ميتة · نشرة بلا حفظ · «تذكّرني» غير مربوط · useEffect فارغ Auth.tsx:83 · تواريخ قانونية · وعود «مدى الحياة/شهادة» · بيانات تواصل وهمية AdminSettings:12,13 · CourseDetail بلا is_published · تعارض Math.max(0/1) · bunny-video-info بلا encodeURIComponent · QuizResult قسمة على صفر و`user!.id` · setTimeout بلا تنظيف · Home:155 play() بلا catch · useAuth:32 بلا try/finally · sitemap بلا /account-deletion · lesson_public_outline بـ SECURITY DEFINER.
13. كود ميت: StatsCounter · admin/glassKit · ParentDashboard · ParentLink · parent-light.css · 40 ملف vite.config.timestamp · `_to_delete` 454MB · 7 ملفات .sql في الجذر.

## متبقّية — التطبيق
2. الوضع الداكن: خلفيات `Colors.white` ثابتة مع نص فاتح → `account_screen.dart:302,1032,1221,1415,1492,1618` و`student_home_screen.dart:413`.
3. `lesson_player_screen.dart` — زر الرجوع يتجمّد لأن `_close()` ينتظر الحفظ بلا مهلة.
5. `Image.network` بلا `cacheWidth` في `student_home_screen.dart:1102`, `course_overview_screen.dart:276`, `quiz_attempt_screen.dart:372`.
6. `lesson_player_screen.dart` — WebView بلا `dispose()`.
7. `quiz_attempt_screen.dart:242` و`quiz_result_screen.dart:64` — نص استثناء إنجليزي خام للطالب.
8. `account_screen.dart:1155` — رسالة خطأ تُبتلع.
9. `brand_launch_gate.dart:32` — 6600ms قبل بدء أي عمل.
10. `supabase_student_quiz_repository.dart:179-186` — خيار رابع فارغ في الأسئلة الثلاثية.
11. `launchUrl` بلا try/catch في 6 مواضع.
12. `login_screen.dart` — «تذكرني» بلا أثر.
13. بسيطة: `setState` بلا mounted في `quiz_attempt_screen.dart:133` · معامل `userId` غير مستخدم · `qm_theme_mode.dart:28-36` بلا try/catch · استعلام الرئيسية O(كورسات×دروس) · وثائق المتجر تذكر 1.0.0.
14. كود ميت: `foundation_screen.dart` · `signed_in_checkpoint_screen.dart` · `subscription_screen.dart` (الأخيرة لسه بتحمل أسعار 79/199/299 القديمة).

## ملاحظة مهمة
الأرقام دي أرقام سطور من 2026-08-31 وممكن تكون اتزحزحت بعد تعديلات 1.1.2 — أكّد بالبحث في الملف قبل أي تعديل.
